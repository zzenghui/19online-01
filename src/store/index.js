import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex)

const actions = {}
const mutations = {}
const state = {}
const getters = {}
const modules = {}

const store = new Vuex.Store({
    actions,
    mutations,
    state,
    getters,
    modules
})

export default store